export const isPromise = (p) => !!p &&
    (p instanceof Promise ||
        ((typeof p === 'object' || typeof p === 'function') &&
            typeof p.then === 'function' &&
            typeof p.catch === 'function' &&
            typeof p.finally === 'function'));
//# sourceMappingURL=index.js.map