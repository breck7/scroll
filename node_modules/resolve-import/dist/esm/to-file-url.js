import { pathToFileURL } from 'url';
export const toFileURL = (p) => typeof p === 'object'
    ? p
    : p.startsWith('file://')
        ? new URL(p)
        : pathToFileURL(p);
//# sourceMappingURL=to-file-url.js.map