import { Box, Text } from 'ink';
import React from 'react';
import { useBailedOut } from './hooks.js';
export const BailedOut = ({ test }) => {
    const bailedOut = useBailedOut(test);
    return bailedOut ? (React.createElement(Box, null,
        React.createElement(Text, { color: "red", bold: true },
            "Bailout!",
            typeof bailedOut === 'string' && ` (${bailedOut})`))) : (React.createElement(React.Fragment, null));
};
//# sourceMappingURL=bailed-out.js.map