export const listenCleanup = (e, ev, fn) => {
    if (!e)
        return () => { };
    e.on(ev, fn);
    return () => {
        e.removeListener(ev, fn);
    };
};
//# sourceMappingURL=listen-cleanup.js.map