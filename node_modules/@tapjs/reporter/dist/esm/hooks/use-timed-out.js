import { useLayoutEffect, useState } from 'react';
import { listenCleanup } from '../listen-cleanup.js';
export const useTimedOut = (t) => {
    const [timedout, setTimedout] = useState(undefined);
    useLayoutEffect(() => listenCleanup(t, 'timeout', extra => setTimedout(extra)), [t.timedOut]);
    return timedout;
};
//# sourceMappingURL=use-timed-out.js.map