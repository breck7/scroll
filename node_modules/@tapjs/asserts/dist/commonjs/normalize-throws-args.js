"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.normalizeThrowsArgs = void 0;
const normalizeThrowsArgs = (defaultMessage, [wanted, message, extra]) => {
    if (typeof wanted === 'string') {
        if (typeof message === 'object' && message) {
            extra = message;
        }
        message = wanted;
        return [undefined, message || defaultMessage, extra || {}];
    }
    if (message && typeof message === 'object') {
        return [wanted, defaultMessage, message];
    }
    return [wanted, message || defaultMessage, extra || {}];
};
exports.normalizeThrowsArgs = normalizeThrowsArgs;
//# sourceMappingURL=normalize-throws-args.js.map